@model Escuela

@{
    ViewData["Title"] = "Información escuela";
    Layout = "Simple";
}
<!--ViewBag y ViewData son dos vistas de la misma información lo que cambia es la manera-->
<!--Con ViewBag podemos mandar elementos extra a los que se encuentra en el modelo-->
<h1>Escuela: @ViewData["Title"]</h1>
<h2>Nombre: @Model.Nombre</h2>
<h3>Tipo Escuela: @Model.TipoEscuela</h3>
<address>
    <p>Dirección: @Model.Dirección</p>
    <p>País: @Model.Pais</p>
    <p>Ciudad: @Model.Ciudad</p>
</address>
<h2>Año fundación: @Model.AñoDeCreación</h2>